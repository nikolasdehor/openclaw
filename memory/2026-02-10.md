# 2026-02-10

## OpenClaw - alinhamento de versão + LaunchAgent
- Config estava em 2026.2.9 enquanto runtime/CLI estava em 2026.2.6-3 (spam de warnings no `openclaw status`).
- Decisão do Nikolas: manter instalação “oficial” via Homebrew Node (`/opt/homebrew/...`) e não NVM.
- Fix aplicado:
  - Atualizado `openclaw` global para 2026.2.9 via `npm i -g openclaw@2026.2.9` (no Node do Homebrew).
  - Removida instalação duplicada antiga do OpenClaw no NVM (levada para o Trash).
  - Problema resultante: o LaunchAgent `~/Library/LaunchAgents/ai.openclaw.gateway.plist` apontava para o caminho antigo do NVM.
  - Correção aplicada pelo Nikolas: ajustar `ProgramArguments` para `/opt/homebrew/lib/node_modules/openclaw/dist/index.js` e reiniciar com `launchctl bootout` + `launchctl bootstrap`.
- Lição: antes de remover instalações antigas, checar o plist do LaunchAgent para não quebrar o gateway.

## Moltbook - digests/crons
- Problema: existiam 2 jobs de digest às 10:00 com formatos diferentes (inconsistência; um vinha em texto corrido e outro estruturado).
- Ação:
  - Desabilitado o digest duplicado `moltbook-digest-nikolas`.
  - Mantido apenas o job `Moltbook - digest diário 10:00` com formato fixo e escaneável (lista numerada + “Por que importa” + link).
  - Pedido do Nikolas: ficar só com o mais completo → desabilitado também o job `Moltbook - check (DMs + feed) a cada 4h`.
  - Digest diário agora inclui também o status de DMs (`0 novas | 0 não lidas | 0 requests`).
- Observação: mensagens “incompletas” vistas depois eram atrasadas do job de check 4h que já tinha disparado antes de ser desligado.

## Nightly Build (rotina diária)
- Pedido do Nikolas: ativar “Nightly Build”.
- Criado cron `Nightly Build (resumo diário)` às 08:05 (America/Sao_Paulo), com:
  - 1 melhoria pequena e segura por dia + status escaneável (WhatsApp + gateway + alertas).
  - Restrição explícita: não rodar updates/config sem pedido.

## Hardening + checklist de skills
- Criado checklist de segurança de skills: `docs/skill-security-checklist.md`.
- Aplicado hardening: `chmod 700 ~/.openclaw/credentials` (antes era 755).

## Finanças (planilha)
- Nikolas confirmou atualização da coluna de fevereiro/2026 na planilha e que vai completar até dezembro; vai mandar “ok” quando terminar para análise pela coluna `Total (R$)`.
